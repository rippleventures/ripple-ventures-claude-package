# Moved to microsoft-office-plugin

This skill has been extracted to the separate `microsoft-office-plugin` so it can be reused across plugins. Install `microsoft-office-plugin` alongside `deals-plugin` to use this skill.

The deals plugin's orchestration skills (`model-builder`, `pitch-deck-creator`, `market-researcher`, etc.) depend on the Office plugin being installed.
